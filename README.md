# first commit


    $ cd /source/to/root/folder
    $ npm install qs
    $ npm install connect
    $ npm install mime
    $ npm install less -g
    $ export PORT=4000
    $ npm install nodemon -g
    $ nodemon src/server.js

